import 'package:flutter/material.dart';

class MentorPage extends StatelessWidget {
  static const routeName = '/mentor_page';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mentors!!!'),
    );
  }
}